# Server Configuration
PORT=3000
NODE_ENV=development

# Frontend URL (for CORS)
FRONTEND_URL=http://localhost:5173

# Optional: IP Whitelist (comma-separated)
# ALLOWED_IPS=127.0.0.1,::1

# Optional: Database Configuration (if you add database later)
# DATABASE_URL=postgresql://user:password@localhost:5432/stock_photo_ai

# Optional: Redis Configuration (for advanced rate limiting)
# REDIS_URL=redis://localhost:6379

# Security Settings
# SESSION_SECRET=your-session-secret-here

# Logging Level
LOG_LEVEL=info

# Optional: Webhook URLs for monitoring
# WEBHOOK_URL_ERROR=https://hooks.slack.com/your-error-webhook
# WEBHOOK_URL_SUCCESS=https://hooks.slack.com/your-success-webhook